/* =====================================================================
   POKEWORLD UNIVERSE — configuração pública do front-end.
   Preencha com a URL e a chave ANON (pública) do seu projeto Supabase.
   Nunca coloque aqui a service_role key.
   Enquanto estiver vazio, o site e o painel admin funcionam em modo
   local (dados salvos no navegador).
   ===================================================================== */
window.PWU_CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: ''
};
